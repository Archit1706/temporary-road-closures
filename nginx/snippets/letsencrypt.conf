# Let's Encrypt ACME challenge configuration
# This allows certbot to verify domain ownership for SSL certificate issuance

location ^~ /.well-known/acme-challenge/ {
    default_type "text/plain";
    root /var/lib/letsencrypt;
}

location = /.well-known/acme-challenge/ {
    return 404;
}
